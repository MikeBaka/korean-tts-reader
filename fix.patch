diff --git a/ios/HanguCore/Sources/HanguCore/Config.swift b/ios/HanguCore/Sources/HanguCore/Config.swift
@@
-    let regionStr = (cognitoIdentity["Default"] as? [String: Any])?["Region"] as? String,
-    let region: AWSRegionType
-    switch regionStr.lowercased() {
-    case "us-west-2":  region = .USWest2
-    case "ap-northeast-2": region = .APNortheast2
-    default:           region = .USEast1       // safe default
-    }
-    return AWSCognitoCredentialsProvider(
-            regionType: region,
-            identityPoolId: poolId
-    )
+    let regionStr = (cognitoIdentity["Default"] as? [String: Any])?["Region"] as? String
+    // pick a region
+    let region: AWSRegionType
+    switch (regionStr ?? "").lowercased() {
+    case "us-west-2":       region = .USWest2
+    case "ap-northeast-2":  region = .APNortheast2
+    default:                region = .USEast1   // safe default
+    }
+    return AWSCognitoCredentialsProvider(
+        regionType: region,
+        identityPoolId: poolId
+    )
diff --git a/ios/HanguCore/Sources/HanguCore/PollyManager.swift b/ios/HanguCore/Sources/HanguCore/PollyManager.swift
@@
-        URLSession.shared.dataTask(with: presignedURL) { data, _, error in
+        URLSession.shared.dataTask(with: presignedURL as URL) { data, _, error in
@@
-        URLSession.shared.dataTask(with: presignedURL) { data, _, error in
+        URLSession.shared.dataTask(with: presignedURL as URL) { data, _, error in
diff --git a/ios/HanguCore/Sources/HanguCore/ReaderViewModel.swift b/ios/HanguCore/Sources/HanguCore/ReaderViewModel.swift
@@
-        let isPlayingPublisher = (ttsPlayer as? MockTTSPlayer)?.isPlayingPublisher ?? ttsPlayer.$isPlaying.eraseToAnyPublisher()
-        let currentTimePublisher = (ttsPlayer as? MockTTSPlayer)?.currentTimePublisher ?? ttsPlayer.$currentTime.eraseToAnyPublisher()
+        let isPlayingPublisher   = ttsPlayer.$isPlaying.eraseToAnyPublisher()
+        let currentTimePublisher = ttsPlayer.$currentTime.eraseToAnyPublisher()
@@
-// ===== Mock-helper section (delete) =====
-/* everything between this comment and the end of MockTTSPlayer
-   was only for earlier unit-test experiments and is now removed */
